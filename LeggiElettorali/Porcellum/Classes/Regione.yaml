metaclasses:
  - totals
  - external

external:
  voti_regionali:
    columns:
      - REGIONE -> Regione
      - COALIZIONE -> Coalizione
      - LISTA -> Lista
      - VOTI_LISTA -> Voti
  voti_liste:
    columns:
      - COALIZIONE -> Coalizione
      - LISTA -> Lista
      - VOTI_LISTA -> Voti
  voti_coalizioni:
    columns:
      - COALIZIONE -> Coalizione
      - VOTI_LISTA -> Voti